[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ml-saham"
version = "0.1.0"
description = "Challenge lab for ai-saham IDX engines/factors (+ curriculum onboarding)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "typer>=0.12",
    "rich>=13.0",
    "numpy>=1.26",
    "pandas>=2.0",
    "scikit-learn>=1.4",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]
ml = [
    "lightgbm>=4.0",
    "polars>=0.20",
    "shap>=0.44",
    "arch>=6.3",
    "hmmlearn>=0.3",
    "networkx>=3.0",
    "hdbscan>=0.8",
    "umap-learn>=0.5",
]

[project.scripts]
ml-saham = "ml_saham.cli.app:main"

[tool.hatch.build.targets.wheel]
packages = ["src/ml_saham"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src", "."]
markers = [
    "live_db: optional tests that need a real ai-saham SQLite (ML_SAHAM_DB)",
]
